#include <stdio.h>

int main(void)
{
    FILE *file = fopen("", "r");

    char *str = malloc( sizeof(char));

    while(fgets(buffer, sizeof(buffer), file))
    {
	if (buffer[256] != '\n')
	{
	    
	}
    }

    fclose(fp);
    return 1;
}
